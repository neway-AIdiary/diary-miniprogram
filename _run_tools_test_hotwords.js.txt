
== 单元：estTokens / isValidWord ==
  ✓ estTokens("王新伟")=5（3 中文字 ×1.5 进位）
  ✓ estTokens("CO2")=2（3 ASCII 字符）
  ✓ 合法词通过
  ✓ 单字符拒绝
  ✓ 带标点拒绝
  ✓ 纯数字拒绝
  ✓ 超长词（13字）拒绝

== 档案名词优先 ==
  ✓ 档案名词排最前
  ✓ 无日记时只有档案词，不凑满预算

== 近十天日记高频词 ==
  ✓ 档案词仍在
  ✓ 近十天出现≥2次的「健身房」入选
  ✓ 超十天的日记不参与统计

== token 预算 100（不超限、不凑满） ==
  ✓ 总 token 98 ≤ 100
  ✓ 档案词超预算即停（收录 14 个，未凑满）

== 按自然日缓存 ==
  ✓ 同日两次 get + 空 ctx：结果一致（基底词命中缓存，ctx 为空，组合可重现）
  ✓ 同日 + 同 ctx 两次调用：结果稳定（buf 每次返回的是新拼接数组，但内容一致）

== 关键词按频次排序 ==
  ✓ 「咖啡馆」（出现2次）入选
  ✓ 词序检查（freq 降序）
  （「读书」未过阈值或非高频词，跳过排序断言）

== 草稿上下文（getContextTerms / build({contextText}) / get(_, {contextText})） ==
  ✓ ctx 优先收「王威」（2 字人名，1 次即收）
  ✓ ctx 不收 3 字词（避免浪费预算；「咖啡馆」由历史高频兜底）
  ✓ 「和王」（含单字停用「和」）不收
  ✓ 「约在」（含单字停用「在」）不收
  ✓ 「在咖」（含单字停用「在」）不收
  ✓ 「碰面」是实词 2 字，被 ctx 收
  ✓ 「威碰」（两个无意义实词组合）由产品设计取舍决定（仅按 STOP_WORDS/SINGLE_STOP_CHARS 过滤）
  ✓ 「王威」只出现 1 次也被收（ctx 不要求高频）
  ✓ 「在这」（含单字停用「在」）不收
  ✓ 「健身」被 ctx 收录
  ✓ 「身房」被 ctx 收录（不依赖 3 字词吸收，由历史高频/档案兜底防重复）
  ✓ 纯单字停用片段 → 返回空数组
  ✓ 「的在了在」全含单字停用 → 返回空数组
  ✓ build({ctx}) 含「王威」
  ✓ 「王威」（ctx）排在「王新伟」（档案）之前
  ✓ 档案词「王新伟」仍在
  ✓ get(_, {ctx}) 含「王威」（2 字专名保住了）
  ✓ 档案词「王新伟」仍在
  ✓ ctx 词「王威」排在档案「王新伟」之前
  ✓ 无 contextText 时档案词排第一（兼容旧行为）
  ✓ 不同 ctx 第一次: 「王威」被注入
  ✓ 不同 ctx 第二次: 「李雷」被注入（ctx 每次按 contextText 重算）
  ✓ 基础词（档案）跨多次 get 仍可见

== 档案顺序可复现（同批跨毫秒） ==
  ✓ 存储数组本身保持录入顺序
  ✓ 同一批档案跨毫秒仍保持录入顺序（实际 甲一→乙二→丙三）
  ✓ 同一批档案共用同一个 updated_at
  ✓ 同一条档案 created_at === updated_at
  ✓ 后一批档案排在前（跨批仍最近优先，实际 丁四→甲一→乙二→丙三）

===== 结果: 47 通过, 0 失败 =====

--ERR--
