[A] warmup 预热与幂等
  ✓ 已授权平台 warmup 成功
  ✓ 首次 warmup 调用 1 次云函数
  ✓ 再次 warmup 命中缓存，不再调云函数
  ✓ 未授权时 warmup 静默跳过（不拉取、不弹框）
  ✓ 未授权不调云函数
[B] 按下即反馈 + 正常录音启动
  ✓ start 后立即广播 connecting 准备态
  ✓ liveText 已清空
  ✓ 录音最终进入 recording 态
  ✓ recorder.start 恰好 1 次
  ✓ 实时链路用 pcm 格式
  ✓ 期间出现过 connecting 广播
[C] 授权期间取消
  ✓ 取消后不启动录音
  ✓ 取消后 connecting/recording 复位
[D] 建连超时录音不受影响（等待 2.7s）
  ✓ 录音在按下后立即启动（不等建连）
  ✓ 按下约 100ms 内已进入录音态
  ✓ 超时后主动 close 连接
  ✓ 超时后录音继续，不重启不中断
  ✓ 超时不重启录音（旧逻辑会回退重录 wav）
  ✓ 全程同一份 pcm 录音，松手后整段识别兜底
  （按→可录音耗时: 119ms 量级，不再包含 2.5s 建连等待）

结果: 19 通过, 0 失败

--ERR--
[voice] WSS 建连超时(2.5s)，关闭连接，本段将走整段识别
